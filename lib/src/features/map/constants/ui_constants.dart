/// ثوابت واجهة المستخدم للخريطة
/// 
/// هذا الملف يحتوي على جميع الثوابت المستخدمة في واجهة الخريطة
/// لضمان الاتساق في التصميم عبر جميع المكونات
class UIConstants {
  /// Border radius للبطاقات
  static const double cardBorderRadius = 12.0;
  
  /// حجم الأيقونات الافتراضي
  static const double iconSize = 20.0;
  
  /// مدة الرسوم المتحركة الافتراضية
  static const Duration animationDuration = Duration(milliseconds: 300);
  
  /// Padding الافتراضي
  static const double defaultPadding = 16.0;
  
  /// Margin الافتراضي
  static const double defaultMargin = 8.0;
  
  /// ارتفاع شريط البحث
  static const double searchBarHeight = 56.0;
  
  /// ارتفاع قائمة العناصر
  static const double listItemHeight = 120.0;
  
  /// الحد الأقصى لارتفاع قائمة العناصر
  static const double maxListHeight = 300.0;
}
